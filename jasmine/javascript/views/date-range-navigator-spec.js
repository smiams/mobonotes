// Generated by CoffeeScript 1.7.1
(function() {
  describe("Date Range Navigator View", function() {
    var dateRangeNavigator;
    dateRangeNavigator = {};
    beforeEach(function() {
      loadFixtures("/views/date-range-navigator.html");
      return dateRangeNavigator = new App.Views.DateRangeNavigator({
        id: "date-range-navigator"
      });
    });
    describe("setStartDate()", function() {
      var dashFormattedDate, humanFormattedDate, testStartDate;
      testStartDate = new Date("2014-07-26 (GMT)");
      dashFormattedDate = "2014-07-26";
      humanFormattedDate = "July 26, 2014";
      it("sets the element's startDate data value in the DOM", function() {
        dateRangeNavigator.setStartDate(testStartDate);
        return expect(dateRangeNavigator.domElement.data().startDate).toBe(dashFormattedDate);
      });
      it("sets the object's startDate attribute", function() {
        dateRangeNavigator.setStartDate(testStartDate);
        return expect(dateRangeNavigator.startDate).toBe(testStartDate);
      });
      return it("sets the domElement's startDate in the #startDate domElement", function() {
        dateRangeNavigator.setStartDate(testStartDate);
        return expect(dateRangeNavigator.domElement.children("#start-date").html()).toBe(humanFormattedDate);
      });
    });
    return describe("startDate", function() {
      return it("returns the value in the DOM element's startDate data value", function() {
        expect(dateRangeNavigator.startDate.getYear()).toEqual(114);
        expect(dateRangeNavigator.startDate.getMonth()).toEqual(6);
        return expect(dateRangeNavigator.startDate.getDate()).toEqual(1);
      });
    });
  });

}).call(this);
