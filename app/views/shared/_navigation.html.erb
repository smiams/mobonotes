<div class="firefox-shim"></div>
<div class="navigation-container">
  <ul class="navigation">
    <li class="<%= @current_tab == notes_path ? "active" : "" %> tab with-label-navigator">
      <a href="#" class="tab">
        <%= @date.strftime("%b #{@date.day.ordinalize}, %Y") %>
      </a>
      <%= render :partial => "shared/label_navigator", :locals => {:labels => @labels} %>
    </li>
    <li class="tab">
      <a href="#" class="tab">Archive</a>
    </li>
    <li class="tab">
      <input type="text">Archive</input>
      <input type="submit" value="Search"/>
    </li>
  </ul>
  <div style="clear:both;"></div>
</div>